<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
      DISABLED during install — this override caused install hangs:

        website.homepage
          -> website.layout
            -> web.login_layout
              -> website.layout   (again via website.login_layout)

      Nested website.layout compilation can lock Odoo while installing.
      Re-enable only with a non-recursive homepage design.
    -->
</odoo>
