<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Employee Experience</title>
<style>
  :root{
    --exp-bg: #f4f5f7;
    --exp-card-bg: #ffffff;
    --exp-border: #e7e9ee;
    --exp-text-primary: #14161a;
    --exp-text-secondary: #6b7280;
    --exp-text-muted: #9aa0ab;
    --exp-zebra: #f7f8fa;
    --exp-radius-lg: 14px;
    --exp-radius-md: 10px;
    --exp-radius-sm: 8px;
    --exp-shadow: 0 1px 2px rgba(16,24,40,0.04);

    --exp-blue: #3b82f6;
    --exp-green: #22c55e;
    --exp-purple: #a855f7;
    --exp-pink: #ec4899;
    --exp-orange: #f97316;
    --exp-red: #ef4444;
  }

  *{ box-sizing: border-box; }

  body{
    margin: 0;
    background: var(--exp-bg);
    color: var(--exp-text-primary);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  }

  .exp_dashboard{
    max-width: 1000px;
    margin: 0 auto;
    padding: 32px 24px 48px;
  }

  /* ---------- Greeting ---------- */
  .exp_greeting_title{
    font-size: 26px;
    font-weight: 700;
    margin: 0 0 4px;
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .exp_greeting_wave{
    font-size: 22px;
  }
  .exp_greeting_subtitle{
    margin: 0 0 24px;
    color: var(--exp-text-secondary);
    font-size: 14px;
  }

  /* ---------- Stat cards ---------- */
  .exp_stats_grid{
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-bottom: 24px;
  }

  .exp_stat_card{
    background: var(--exp-card-bg);
    border: 1px solid var(--exp-border);
    border-radius: var(--exp-radius-lg);
    box-shadow: var(--exp-shadow);
    padding: 18px 18px;
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 12px;
  }

  .exp_stat_label{
    font-size: 13px;
    color: var(--exp-text-secondary);
    margin: 0 0 10px;
  }

  .exp_stat_value{
    font-size: 26px;
    font-weight: 700;
    margin: 0;
    line-height: 1;
  }

  .exp_stat_icon{
    width: 40px;
    height: 40px;
    min-width: 40px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
  }
  .exp_stat_icon svg{ width: 18px; height: 18px; }

  .exp_icon_blue   { background: var(--exp-blue); }
  .exp_icon_green  { background: var(--exp-green); }
  .exp_icon_purple { background: var(--exp-purple); }
  .exp_icon_pink   { background: var(--exp-pink); }
  .exp_icon_orange { background: var(--exp-orange); }
  .exp_icon_red    { background: var(--exp-red); }

  /* ---------- Content panels ---------- */
  .exp_content_grid{
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    align-items: start;
  }

  .exp_panel{
    background: var(--exp-card-bg);
    border: 1px solid var(--exp-border);
    border-radius: var(--exp-radius-lg);
    box-shadow: var(--exp-shadow);
    padding: 20px;
  }

  .exp_panel_title{
    font-size: 15px;
    font-weight: 700;
    margin: 0 0 14px;
  }

  .exp_border_line{
    border: none;
    border-top: 1px solid var(--exp-border);
    margin: 14px 0;
  }

  .exp_list{
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .exp_list_empty{
    color: var(--exp-text-muted);
    font-size: 13px;
    padding: 12px 4px;
  }

  /* Campaign rows */
  .exp_campaign_row{
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 12px 14px;
    border-radius: var(--exp-radius-sm);
  }
  .exp_campaign_row:nth-child(even){ background: var(--exp-zebra); }

  .exp_campaign_name{
    font-size: 14px;
    font-weight: 600;
    margin: 0 0 3px;
  }
  .exp_campaign_status{
    font-size: 12.5px;
    color: var(--exp-text-secondary);
    margin: 0;
  }

  .exp_campaign_meta{
    text-align: right;
  }
  .exp_campaign_progress{
    font-size: 13px;
    font-weight: 700;
    color: var(--exp-pink);
    margin: 0 0 3px;
  }
  .exp_campaign_progress.exp_no_progress{
    color: var(--exp-text-muted);
  }
  .exp_campaign_action{
    font-size: 12.5px;
    color: var(--exp-text-secondary);
    margin: 0;
  }

  /* Celebration rows */
  .exp_celebration_row{
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 10px 14px;
    border-radius: var(--exp-radius-sm);
  }
  .exp_celebration_row:nth-child(even){ background: var(--exp-zebra); }

  .exp_celebration_avatar{
    width: 36px;
    height: 36px;
    min-width: 36px;
    border-radius: 50%;
    background: #fbeaf0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 17px;
  }

  .exp_celebration_info{
    flex: 1;
    min-width: 0;
  }
  .exp_celebration_name{
    font-size: 14px;
    font-weight: 600;
    margin: 0 0 2px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .exp_celebration_sub{
    font-size: 12.5px;
    color: var(--exp-text-secondary);
    margin: 0;
  }

  .exp_celebration_days{
    font-size: 13px;
    color: var(--exp-text-secondary);
    white-space: nowrap;
  }

  /* ---------- Loading / error states ---------- */
  .exp_loading, .exp_error{
    font-size: 13px;
    color: var(--exp-text-muted);
    padding: 12px 4px;
  }
  .exp_error{ color: var(--exp-red); }

  /* ---------- Responsive ---------- */
  @media (max-width: 760px){
    .exp_stats_grid{ grid-template-columns: repeat(2, 1fr); }
    .exp_content_grid{ grid-template-columns: 1fr; }
  }
  @media (max-width: 460px){
    .exp_stats_grid{ grid-template-columns: 1fr; }
  }
</style>
</head>
<body>

<div class="exp_dashboard">

  <h1 class="exp_greeting_title"><span id="exp_greeting_text">Good morning, Admin!</span><span class="exp_greeting_wave">👋</span></h1>
  <p class="exp_greeting_subtitle">Here's what's happening in Employee Experience today</p>

  <!-- Stat cards -->
  <div class="exp_stats_grid">

    <div class="exp_stat_card">
      <div>
        <p class="exp_stat_label">Videos Watched Today</p>
        <p class="exp_stat_value" id="exp_stat_videos">–</p>
      </div>
      <div class="exp_stat_icon exp_icon_blue">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="23 7 16 12 23 17 23 7"></polygon><rect x="1" y="5" width="15" height="14" rx="2" ry="2"></rect></svg>
      </div>
    </div>

    <div class="exp_stat_card">
      <div>
        <p class="exp_stat_label">Messages Sent This Month</p>
        <p class="exp_stat_value" id="exp_stat_messages">–</p>
      </div>
      <div class="exp_stat_icon exp_icon_green">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
      </div>
    </div>

    <div class="exp_stat_card">
      <div>
        <p class="exp_stat_label">Active Campaigns</p>
        <p class="exp_stat_value" id="exp_stat_campaigns">–</p>
      </div>
      <div class="exp_stat_icon exp_icon_purple">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
      </div>
    </div>

    <div class="exp_stat_card">
      <div>
        <p class="exp_stat_label">Upcoming Celebrations (30 days)</p>
        <p class="exp_stat_value" id="exp_stat_celebrations">–</p>
      </div>
      <div class="exp_stat_icon exp_icon_pink">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
      </div>
    </div>

    <div class="exp_stat_card">
      <div>
        <p class="exp_stat_label">Knowledge Items Published</p>
        <p class="exp_stat_value" id="exp_stat_knowledge">–</p>
      </div>
      <div class="exp_stat_icon exp_icon_orange">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path></svg>
      </div>
    </div>

    <div class="exp_stat_card">
      <div>
        <p class="exp_stat_label">Recognitions This Month</p>
        <p class="exp_stat_value" id="exp_stat_recognitions">–</p>
      </div>
      <div class="exp_stat_icon exp_icon_red">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path></svg>
      </div>
    </div>

  </div>

  <!-- Panels -->
  <div class="exp_content_grid">

    <div class="exp_panel">
      <h2 class="exp_panel_title">Active Campaigns</h2>
      <div class="exp_list" id="exp_campaigns_list">
        <div class="exp_loading">Loading campaigns…</div>
      </div>
    </div>

    <div class="exp_panel">
      <h2 class="exp_panel_title">Upcoming Celebrations</h2>
      <div class="exp_list" id="exp_celebrations_list">
        <div class="exp_loading">Loading celebrations…</div>
      </div>
    </div>

  </div>

</div>

<script>
/* =========================================================================
   DATA LAYER
   Everything below reads from the `calendar.event` table, filtered by
   `event_type` (e.g. "campaign", "birthday", "work_anniversary",
   "retirement"). Replace `fetchCalendarEvents()` with your real API/DB
   call — the rest of the rendering code only depends on the shape
   documented below.

   Expected row shape from calendar.event:
   {
     id: string,
     event_type: "campaign" | "birthday" | "work_anniversary" | "retirement",
     title: string,               // e.g. "Birthday Celebration Campaign" or employee name
     status: string,               // e.g. "active", "scheduled", "completed"
     progress_pct: number|null,    // campaigns only, 0-100 or null
     department: string|null,      // celebrations only
     event_date: string,           // ISO date, used to compute "days away"
     department_or_type_label: string|null
   }
   ========================================================================= */

async function fetchCalendarEvents(eventTypes) {
  // ---- Replace this block with your real backend call, e.g.: -------------
  // const res = await fetch(`/api/method/calendar.get_events?event_type=${eventTypes.join(',')}`);
  // if (!res.ok) throw new Error('Failed to load calendar.event');
  // return await res.json();
  // --------------------------------------------------------------------
  // Demo fallback data below mirrors calendar.event rows so the page
  // renders standalone until the real endpoint is wired in.
  const demoRows = [
    { id: 'c1', event_type: 'campaign', title: 'Birthday Celebration Campaign', status: 'active', progress_pct: 82, event_date: '2026-08-19' },
    { id: 'c2', event_type: 'campaign', title: 'Work Anniversary Campaign', status: 'active', progress_pct: 74, event_date: '2026-08-19' },
    { id: 'c3', event_type: 'campaign', title: 'Christmas 2026', status: 'scheduled', progress_pct: null, event_date: '2026-12-25' },

    { id: 'e1', event_type: 'birthday', title: 'Emma Johnson', department_or_type_label: 'Birthday · Marketing', event_date: '2026-08-21' },
    { id: 'e2', event_type: 'work_anniversary', title: 'James Okafor', department_or_type_label: 'Work Anniversary · Engineering', event_date: '2026-08-24' },
    { id: 'e3', event_type: 'work_anniversary', title: 'Sarah Chen', department_or_type_label: 'Work Anniversary · Sales', event_date: '2026-08-26' },
    { id: 'e4', event_type: 'birthday', title: 'Michael Brown', department_or_type_label: 'Birthday · HR', event_date: '2026-08-29' },
    { id: 'e5', event_type: 'retirement', title: 'Lisa Anderson', department_or_type_label: 'Retirement · Finance', event_date: '2026-09-02' },
  ];
  return demoRows.filter(r => eventTypes.includes(r.event_type));
}

const EXP_CELEBRATION_ICONS = {
  birthday: '🎂',
  work_anniversary: '🎉',
  retirement: '🏅',
};

function exp_daysFromToday(isoDate) {
  const today = new Date();
  today.setHours(0, 0, 0, 0);
  const target = new Date(isoDate);
  target.setHours(0, 0, 0, 0);
  const diffMs = target - today;
  return Math.round(diffMs / (1000 * 60 * 60 * 24));
}

function exp_renderCampaigns(rows) {
  const container = document.getElementById('exp_campaigns_list');
  if (!rows.length) {
    container.innerHTML = '<div class="exp_list_empty">No active campaigns.</div>';
    return;
  }
  container.innerHTML = rows.map(row => {
    const hasProgress = row.progress_pct !== null && row.progress_pct !== undefined;
    const progressLabel = hasProgress ? `${row.progress_pct}%` : 'N/A';
    const actionLabel = row.status === 'active' ? 'Pause' : (row.status === 'scheduled' ? 'Scheduled' : row.status);
    const statusLabel = row.status.charAt(0).toUpperCase() + row.status.slice(1);
    return `
      <div class="exp_campaign_row">
        <div>
          <p class="exp_campaign_name">${row.title}</p>
          <p class="exp_campaign_status">${statusLabel}</p>
        </div>
        <div class="exp_campaign_meta">
          <p class="exp_campaign_progress ${hasProgress ? '' : 'exp_no_progress'}">${progressLabel}</p>
          <p class="exp_campaign_action">${actionLabel}</p>
        </div>
      </div>`;
  }).join('');
}

function exp_renderCelebrations(rows) {
  const container = document.getElementById('exp_celebrations_list');
  if (!rows.length) {
    container.innerHTML = '<div class="exp_list_empty">No upcoming celebrations.</div>';
    return;
  }
  const sorted = [...rows].sort((a, b) => new Date(a.event_date) - new Date(b.event_date));
  container.innerHTML = sorted.map(row => {
    const icon = EXP_CELEBRATION_ICONS[row.event_type] || '📌';
    const days = exp_daysFromToday(row.event_date);
    const daysLabel = days <= 0 ? 'Today' : `${days} day${days === 1 ? '' : 's'}`;
    return `
      <div class="exp_celebration_row">
        <div class="exp_celebration_avatar">${icon}</div>
        <div class="exp_celebration_info">
          <p class="exp_celebration_name">${row.title}</p>
          <p class="exp_celebration_sub">${row.department_or_type_label || ''}</p>
        </div>
        <div class="exp_celebration_days">${daysLabel}</div>
      </div>`;
  }).join('');
}

function exp_setStat(id, value) {
  document.getElementById(id).textContent = value;
}

async function exp_init() {
  try {
    const campaignRows = await fetchCalendarEvents(['campaign']);
    exp_renderCampaigns(campaignRows);
    exp_setStat('exp_stat_campaigns', campaignRows.filter(r => r.status === 'active').length);

    const celebrationTypes = ['birthday', 'work_anniversary', 'retirement'];
    const celebrationRows = await fetchCalendarEvents(celebrationTypes);
    // Keep only celebrations within the next 30 days for the list + stat.
    const upcoming = celebrationRows.filter(r => {
      const d = exp_daysFromToday(r.event_date);
      return d >= 0 && d <= 30;
    });
    exp_renderCelebrations(upcoming);
    exp_setStat('exp_stat_celebrations', upcoming.length);

    // These three stats aren't sourced from calendar.event in the
    // reference design — wire them to their own endpoints as needed.
    exp_setStat('exp_stat_videos', 18);
    exp_setStat('exp_stat_messages', 47);
    exp_setStat('exp_stat_knowledge', 8);
    exp_setStat('exp_stat_recognitions', 43);
  } catch (err) {
    console.error(err);
    document.getElementById('exp_campaigns_list').innerHTML = '<div class="exp_error">Could not load campaigns.</div>';
    document.getElementById('exp_celebrations_list').innerHTML = '<div class="exp_error">Could not load celebrations.</div>';
  }
}

exp_init();
</script>

</body>
</html>