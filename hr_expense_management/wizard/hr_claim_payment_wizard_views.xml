<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_hr_claim_payment_wizard_form" model="ir.ui.view">
        <field name="name">hr.claim.payment.wizard.form</field>
        <field name="model">hr.claim.payment.wizard</field>
        <field name="arch" type="xml">
            <form string="Register Claim Payment">
                <group>
                    <field name="claim_id"/>
                    <field name="currency_id" invisible="1"/>
                    <field name="amount"/>
                    <field name="payment_method"/>
                    <field name="payment_date"/>
                    <field name="reference"/>
                    <field name="notes"/>
                </group>
                <footer>
                    <button name="action_register_payment" type="object" string="Register Payment" class="btn-primary"/>
                    <button string="Cancel" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>

